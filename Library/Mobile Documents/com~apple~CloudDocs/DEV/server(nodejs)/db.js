const config = {
    database  : {
        host : 'localhost',
        user : 'root',
        password : '', // In default, XAMPP hav no password
        port : 3360,
        db : 'myrestaurant'

    }
}

module.exports = config